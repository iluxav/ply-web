{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "brand:build": "node scripts/generate-brand-assets.mjs",
    "sync-content": "rm -rf .content && mkdir -p .content && cp -a ../docs .content/docs && cp ../install.sh public/install.sh && node ../scripts/build-skill.mjs",
    "predev": "npm run sync-content",
    "dev": "next dev",
    "build": "next build",
    "start": "node .next/standalone/server.js",
    "lint": "eslint",
    "test": "vitest run",
    "prebuild": "npm run sync-content",
    "postbuild": "rm -rf .next/standalone/.next/static .next/standalone/public .next/standalone/.content && cp -r .next/static .next/standalone/.next/static && cp -r public .next/standalone/public && cp -r .content .next/standalone/.content"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1119.0",
    "gray-matter": "^4.0.3",
    "highlight.js": "^11.12.0",
    "marked": "^18.0.10",
    "next": "16.3.2",
    "postgres": "^3.4.9",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "smol-toml": "^1.8.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.2",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
